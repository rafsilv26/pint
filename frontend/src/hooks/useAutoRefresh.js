export function useAutoRefresh() {
  // Os dados são atualizados pelo carregamento inicial e pelas ações explícitas
  // de cada página, evitando recargas periódicas que interrompem a interface.
  return undefined
}
